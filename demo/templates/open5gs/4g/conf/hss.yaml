db_uri: mongodb://localhost/open5gs
logger:
    file:
      path: ${__top_dir}/persist/${__node_name}/var/log/hss.log
    level: ${open5gs_log_level}

global:
  max:
    ue: 1024  # The number of UE can be increased depending on memory size.
#    peer: 64

hss:
  freeDiameter: /home/jgiovatto/Devel/open5gs/install/etc/freeDiameter/hss.conf
  metrics:
    server:
      - address: 127.0.0.8
        port: 9090
#  sms_over_ims: "sip:smsc.mnc001.mcc001.3gppnetwork.org:7060;transport=tcp"
#  use_mongodb_change_stream: true
